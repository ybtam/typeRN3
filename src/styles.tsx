import React from "react";
import {StyleSheet} from "react-native";

export const textInputArea = StyleSheet.create({
    container: {
        flex: 1,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    },

    textFieldContainer:{
        width: 200,
        marginTop:10
    }
});

export const tabScreenStyle = StyleSheet.create({
    container:{
        flex: 1,
        // justifyContent: 'space-between'
    },
});